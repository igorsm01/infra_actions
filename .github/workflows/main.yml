
name CI:


on:
  push:
    branches: [main]
  pull_request:
    branches: [main]


jobs:
  build:
    runs-on: ubuntu-latest

  steps:
    - uses: actions/checkout@v3
      with: 
        repository: ${{ github.repository }}
  
    - name: Run one-line script
      run: echo hello 

    - name: Run multiple
      run: |
        echo add other
        echo test deploy

